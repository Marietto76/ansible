# dnf install pip
# pip install ansible-lint


# ansible-galaxy collection list
# ansible-lint playbooks/patching_limit_and_request.yml

/collections/requirements
  collections:
  - name: kubernetes.core

/root/ansible.cfg
  [defaults]
  display_skipped_hosts = false
  #inventory = inventories/inventory
  inventory = inventory/desa/inventory
  ###my definitions ##
  roles_path = ./roles

 ansible-galaxy collection install ansible.posix 

[ansible@ecdevd2ldap-master ~]$ ssh-copy-id ansible@ecdevd2ldap-master.day2.com
hacer el ssh copy id al propio host


cambiar la ip address de /32 a /24

mpepinos@fedora:~/bp_devops/ans-aut-arqPlataf-directory_server$ sudo adduser ansible
mpepinos@fedora:~/bp_devops/ans-aut-arqPlataf-directory_server$ sudo passwd ansible

echo "ansible ALL=(ALL) NOPASSWD:ALL" > /etc/sudoers.d/ansible

ssh-keygen -t rsa -b 2048

ssh-copy-id ansible_user@remote_machine
